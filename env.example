# Copy to .env (gitignored). Real values live only in local env.
CONTEXT_DEV_API_KEY=
CONVEX_DEPLOY_KEY=
CONVEX_URL=
# Fallback LLM key behind the same extraction interface (sponsor endpoint switch)
EXTRACTION_API_KEY=
